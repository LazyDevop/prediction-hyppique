- source_spec: none
  summary: Port the reconstructed scoring engine to Dart (mobile/lib/engine/scoring.dart, mobile/test/engine/scoring_test.dart) and rebuild mobile/lib/screens/race_config_screen.dart.
  evidence: Split from the corrupted-files reconstruction intent — backend (Python) and mobile (Dart) are independently shippable layers; the Dart port should follow once the Python engine is validated and stable, per cahier_des_charges_app_mobile.md section 5.2.

- source_spec: `_bmad-output/implementation-artifacts/spec-restore-backend-scoring-engine.md`
  summary: HIGH IMPACT — backend/app/api/routes_analyse.py line 49 (`resultats=[horse.__dict__ for horse in results]`) leaks virtual-outsider placeholders (`num_pmu=None`, `nom="Outsider virtuel"`) into the `/analyse` API response whenever `nb_partants_course` exceeds the number of horses submitted — the common case for that feature. It should filter to real horses only (e.g. `results[:len(horses)]` or `num_pmu is not None`), mirroring how the same file already slices correctly for `build_combinaisons` on the line just above.
  evidence: Verification-gap review of the scoring-engine restoration surfaced this. routes_analyse.py is intact/untouched by this story (pre-existing bug, not introduced by the reconstruction) but was unreachable while scoring.py was corrupted — now that scoring.py works, the bug is live and would show fabricated horses to real bettors via the mobile app.
  resolved: Fixed directly on user request (commit following spec-restore-backend-scoring-engine.md's completion) — routes_analyse.py now filters `resultats` to `horse.num_pmu is not None`, with a regression test in backend/tests/test_routes_analyse.py.

- source_spec: `_bmad-output/implementation-artifacts/spec-restore-backend-scoring-engine.md`
  summary: backend/app/engine/combinatoire.py has zero test coverage (no test file imports or calls it, directly or via the route).
  evidence: Verification-gap review noted this; combinatoire.py was intact before this story and untouched by it — pre-existing gap, not caused by the scoring-engine restoration.
  resolved: Fixed via spec-combinatoire-test-coverage.md — 17 tests added in backend/tests/test_combinatoire.py, reviewed and committed (a9a93c1), pushed.

- source_spec: `_bmad-output/implementation-artifacts/spec-restore-backend-scoring-engine.md`
  summary: `analyse_course`'s `params` dict is merged over `DEFAULT_PARAMETERS` with no key validation — a mistyped key is silently dropped, and hostile/malformed values (negative `shrink`, `age_min > age_max`) can crash the request or silently produce wrong scores.
  evidence: Blind-hunter and edge-case-hunter reviews both flagged this class of issue. Not in scope for a faithful port of the reference engine (neither the JS prototypes nor the cahier des charges validate params) — worth a dedicated input-validation pass (e.g. a Pydantic model for `params` at the API boundary) if it ever matters in practice.

- source_spec: `_bmad-output/planning-artifacts/architecture/architecture-prediction-hyppique-2026-09-04/ARCHITECTURE-SPINE.md`
  summary: HIGH IMPACT — backend/app/api/routes_analyse.py's `/analyse` endpoint ships `resultats=[horse.__dict__ for horse in results]` typed as a bare `List[Dict]`, bypassing the already-defined `HorseOut` Pydantic schema entirely (AnalyseOut.resultats never validates against it). Should construct `HorseOut(...)` (or `.model_validate()`) per horse instead of dumping the dataclass `__dict__` raw.
  evidence: Adversarial review of the architecture spine (AD-4) surfaced this as a live inconsistency, not hypothetical — two future spine-compliant stories could each "fix" this differently (one extends via `__dict__`, one enforces `HorseOut` strictly) and ship incompatible payloads. routes_analyse.py is intact/pre-existing, not touched by the scoring-engine restoration story.
  resolved: Fixed via spec-1-3-typed-analyse-response.md — AnalyseOut.resultats is now List[HorseOut], built via HorseOut.model_validate(horse) (from_attributes on the model itself per code review). Regression tests assert the exact field set, correct per-horse value mapping, the all-filtered-out edge case, and Optional-field-None propagation. 60/60 backend tests pass.

- source_spec: `_bmad-output/planning-artifacts/architecture/architecture-prediction-hyppique-2026-09-04/ARCHITECTURE-SPINE.md`
  summary: Mobile side is missing a `niveauCoefficient()`-equivalent lookup (it has `terrainCoefficient()` but nothing for niveau labels), and both backend and mobile currently collapse "genuinely missing" and "unrecognized label" into the same neutral coefficient with no distinguishing log/signal (AD-5).
  evidence: Adversarial review of the architecture spine surfaced this — the two cases are indistinguishable today in logs or the UI's "terrain inconnu" counter (cahier backend §7.5, PRD FR-5), which could hide real data-quality problems (a typo'd label silently treated as "unknown, that's fine" instead of flagged).

- source_spec: `_bmad-output/implementation-artifacts/spec-1-1-engine-cases-fixture.md`
  summary: `fixtures/engine_cases.json`'s assertion DSL has no tolerance-based float equality type (`field_equals` is only ever used with an integer field today) and no formal JSON schema/README documenting the case format — both real gaps for a file explicitly meant to be a cross-language (Python + future Dart) contract, but not needed by any of the 6 current cases.
  evidence: Blind-hunter review of Story 1.1. Deliberately out of scope for a mechanical extraction of already-passing tests — revisit when a future fixture case actually needs float-tolerance equality, or when the Dart consumer (blocked — see the mobile Dart-port entry above) is finally built and a schema becomes worth enforcing mechanically.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-1-engine-cases-fixture.md`
  summary: The assertion DSL's `params`/`mode_recence` override plumbing in `_run_fixture_case` is unexercised — none of the 6 current fixture cases sets a non-default `params` or `mode_recence`, even though the schema supports both.
  evidence: Blind-hunter + edge-case-hunter review of Story 1.1. Not a bug (nothing currently relies on the untested path producing a specific result), just unused optional capability — worth a case exercising it once a future story's test actually needs a non-default parameter or récence mode.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-2-engine-constants-fixture.md`
  summary: `fixtures/engine_cases.json`'s loader (Story 1.1) still raises a bare `KeyError`/crashes the whole test module on a missing/invalid fixture file or a fixture case missing a required key — the same class of fragility Story 1.2's review caught and fixed for `engine_constants.json`'s loader, but fixing it in `engine_cases.json`/its consuming tests would have meant touching Story 1.1's already-frozen code, out of bounds for this story.
  evidence: Blind-hunter + edge-case-hunter review of Story 1.2, generalized to the sibling fixture. Low real risk (a malformed fixture file is a local dev-time mistake, not a production path), but worth a small consistency pass once both fixtures are touched by the same future story.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-2-engine-constants-fixture.md`
  summary: The "mobile engine is corrupted/blocked" fact is now duplicated independently across 3 files (`fixtures/engine_cases.json`'s `$comment`, `fixtures/engine_constants.json`'s `$comment`, and this file) — each will need separate updating once the mobile engine is actually reconstructed, which is itself a drift risk despite AD-2 existing specifically to prevent drift elsewhere.
  evidence: Blind-hunter review of Story 1.2. Minor documentation hygiene, not a functional gap — worth centralizing (e.g. both fixture `$comment`s referencing this file by path instead of repeating the claim) whenever either fixture is next touched.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-2-engine-constants-fixture.md`
  summary: `constants.py`'s `terrain_coefficient(label)` function (the actual runtime consumer of `TERRAIN_COEFFICIENTS_GRASS`/`_PSF`) has no direct unit test anywhere in the backend suite — Story 1.2 only validates the raw tables match the fixture, never that the lookup function built on top of them still resolves a label correctly.
  evidence: Blind-hunter review of Story 1.2. Pre-existing gap (the function predates this story), not introduced by it — worth a small dedicated test once someone is next in this file.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-3-typed-analyse-response.md`
  summary: `/analyse`'s `course_id` branch (the `repository.get_horses_for_course` path) is untested — all of test_routes_analyse.py's tests post a manual `chevaux` list, so the `HorseOut.model_validate(horse)` fix (and the response typing generally) is unverified for the DB-backed code path, which shares the exact same construction line.
  evidence: Blind-hunter review of Story 1.3. Requires standing up a course + participations in the temp-db (pattern already established in test_main.py) — more setup than this bugfix story's scope; worth a dedicated test once a story next touches the course_id path.
  resolved: Partially — Story 2.1 adds direct repository-level coverage of `get_horses_for_course`'s field mapping (the exact data that would flow through the `course_id` branch). Still open: no test posts `{"course_id": N}` to the live `/analyse` endpoint itself end-to-end.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-3-typed-analyse-response.md`
  summary: No mechanical test ties `HorseOut`'s field set to `HorseAnalysis`'s field set (e.g. `dataclasses.fields(HorseAnalysis)` minus known-internal names vs. `HorseOut.model_fields`) — the exact class of bug just fixed (a schema silently drifting from the dataclass it mirrors) could recur with nothing catching it mechanically, only another manual code review.
  evidence: Blind-hunter review of Story 1.3. A real hardening opportunity but adds meaningful complexity (deciding which HorseAnalysis fields are "intentionally internal" vs. "forgot to expose") — worth it once HorseAnalysis gains a field for the first time post-fix, as a concrete trigger to build the check against.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-3-typed-analyse-response.md`
  summary: `/analyse` has no error handling around `HorseOut.model_validate(horse)` — if a `HorseAnalysis` ever violated `HorseOut`'s types (a bug elsewhere), the route would now surface a raw, unhandled `pydantic.ValidationError` as a 500 rather than a controlled error response.
  evidence: Edge-case-hunter review of Story 1.3. A behavior change introduced by fixing AD-4 (the old `.__dict__` dump could never fail this way) — low likelihood since it requires an upstream engine bug, but worth a `try/except` -> clean `HTTPException(500, ...)` wrapper if `/analyse` ever gets broader hardening attention.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-4-validate-engine-params.md`
  summary: `EngineParamsIn`'s bounds have no upper ceiling on `coef_inedit`, `bankroll`, `malus_incident`, or `sensibilite_poids` — an absurd but technically-valid value (e.g. `coef_inedit=1000`, `bankroll=1e12`) is accepted and silently produces an equally absurd score/mise, no guardrail. Also: `Field`s lack `description=` metadata for the auto-generated OpenAPI docs, and `AnalyseIn`/`HorseIn`/`PerformanceIn` still don't set `extra="forbid"` — a typo'd top-level field (e.g. `distence`) is silently ignored while the identical mistake inside `params` is now caught, an inconsistency in the fix's reach.
  evidence: Blind-hunter + edge-case-hunter review of Story 1.4. `contraste`'s ceiling was fixed now (real overflow risk, cheap fix) — the others are "implausible input produces implausible output," not a crash or silent corruption of a different field, so left for a future dedicated hardening pass rather than expanding this story's scope further.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-4-validate-engine-params.md`
  summary: No test covers `params: {}` (explicit empty object) vs. omitting `params` entirely — both should behave identically (all defaults) but the equivalence isn't verified; no test covers a non-numeric value inside `params` (e.g. `{"shrink": "abc"}`) to lock in Pydantic's coercion/rejection behavior; no test asserts the actual error-message content of a 422 response (only the status code).
  evidence: Blind-hunter + verification-gap review of Story 1.4. None are false-pass risks (a real regression would still fail loudly, just without a test naming it precisely) — lower priority than the bound-coverage and partial-override gaps that were fixed directly.

- source_spec: `_bmad-output/implementation-artifacts/spec-2-1-repository-test-coverage.md`
  summary: PRODUCT-RELEVANT, not just a test gap — `terrain` and `niveau` are currently never written to a non-NULL/non-None value by ANY real ingestion path in the codebase. `repository.save_historique` hardcodes `terrain=None`; `repository.ajouter_performance_historique_backfill` hardcodes `terrain=None`; `pmu_client.PerformancePassee` (the daily-ingestion dataclass) has no `terrain` field at all — `get_historique` never extracts one from the PMU API response; `repository.get_horses_for_course` hardcodes `niveau=None` on every `Performance` it builds. Consequence: the terrain-coefficient and niveau-coefficient adjustments in `scoring.py`'s `compute_note` are currently INERT for any course sourced through real ingestion — `c_terr` and `c_niv` are always neutral (1.0) in production today, regardless of the actual terrain/niveau of a race.
  evidence: Verification-gap review of Story 2.1 — surfaced because the new `get_horses_for_course` test had to insert `PerformanceHistorique` rows directly via the ORM (bypassing `repository.py`'s own write methods entirely) to exercise a non-null `terrain` at all, since no code path produces one. This goes beyond the already-documented "terrain sometimes missing from PMU" limitation (cahier backend §10) — it's currently "terrain/niveau are ALWAYS missing via ingestion," a materially stronger statement. Niveau-from-allocation derivation (cahier backend §7.2, PRD Open Question 5) is the known plan to eventually populate niveau; terrain has no such plan yet (cahier backend §10 concluded no free/reliable backfill source exists). Not fixed here — this is a data-sourcing gap, not a `repository.py` bug, and touches the ingestion pipeline (Epic 2's own remaining stories) more than this test-coverage story.

- source_spec: `_bmad-output/implementation-artifacts/spec-2-1-repository-test-coverage.md`
  summary: Several `repository.py` branches remain untested after Story 2.1's core-behavior pass: `get_cheval_by_id` (never called by any test); `update_resultats`'s `arrivee_definitive=False` path and its "partant not present" skip branch; `save_historique`/`ajouter_performance_historique_backfill` called with multiple performances for one horse in a single call; `save_partants`/`ajouter_performance_historique_backfill` writing fields beyond the one or two asserted per test (driver, entraineur, poids, incident, discipline, distance, nb_participants); calling repository methods with a nonexistent course/course_id (would raise a bare AttributeError — currently only ever called after routes already validate existence, so low real risk).
  evidence: Blind-hunter + edge-case-hunter review of Story 2.1. The highest-value gaps (HorseAnalysis field mapping, cross-course horse identity, hippodrome scoping, empty/multi-result set coverage, upsert field completeness, `save_historique`'s orphaned-`num_pmu` skip branch) were fixed directly during this story's review; these remaining ones are lower-value/narrower branches worth a follow-up pass rather than expanding this story further.

- source_spec: `_bmad-output/implementation-artifacts/spec-2-2-ingest-daily-test-coverage.md`
  summary: PRODUCT-RELEVANT, not just a test gap — `pmu_client.get_programme` is called with NO try/except guard in both `ingest_programme_du_jour` (ingest_daily.py:13) and `ingest_resultats_veille` (ingest_daily.py:40), unlike every other PMU call in the same functions (`get_participants`/`get_historique`, both explicitly guarded). A single flaky/failed programme fetch for one date — e.g. a PMU API hiccup on `today+1` — propagates uncaught out of `ingest_programme_du_jour`, aborts `run()` entirely, and skips every later step including `ingest_resultats_veille` for yesterday. That means a single bad day can silently prevent the previous day's results from ever being finalized in that run, with only whatever cron-level error alerting exists (none observed in this codebase) as a safety net.
  evidence: Blind-hunter + edge-case-hunter review of Story 2.2, independently, both flagged the same asymmetry. Not fixed here — `ingest_daily.py` is read-only this story (pure test-coverage addition); this is a real resilience gap in the job itself, worth a dedicated fix (wrap both `get_programme` calls in their own try/except, matching the pattern already used for `get_participants`) in a follow-up story, not a test.

- source_spec: `_bmad-output/implementation-artifacts/spec-2-2-ingest-daily-test-coverage.md`
  summary: Lower-priority test gaps left open after Story 2.2's review: no test uses `caplog`/a patched logger to confirm the two swallowed-exception paths actually call `logger.exception` (only the swallow-and-continue side effect is verified); Test 2's failure scenario (course 1 fails, course 2 succeeds) is the mirror image of the frozen spec's I/O matrix wording (course 2 of 2 fails) — functionally equivalent, not literally matching; no test exercises `ingest_resultats_veille` with an unfinalized course dated exactly `today` (i.e. `veille + 1`) to directly prove the strict `<` cutoff at the job-call-site level (trusted via Story 2.1's repository-level coverage of `get_unfinalized_courses_before` instead); the temp-SQLite fixture's cleanup is best-effort or `Repository` never explicitly disposes its SQLAlchemy engine (same class of finding as Story 2.1, not new).
  evidence: Blind-hunter review of Story 2.2. None are false-pass risks for the behaviors this story targets — the higher-value gaps (numero_reunion-only match, multi-date programme caching, the numero_reunion/numero_course XOR-None branch, the get_historique-specific failure path, run()'s default days_ahead) were fixed directly during this story's review.

- source_spec: `_bmad-output/implementation-artifacts/spec-2-3-backfill-historique-test-coverage.md`
  summary: PRODUCT-RELEVANT, not just a test gap — same class of finding as Story 2.2's unguarded `get_programme`. In both `backfill_plage` (backfill_historique.py:44-64) and `backfill_cheval_par_hippodromes_connus` (backfill_historique.py:102-118), only the `open_pmu_client.get_arrivees` fetch is wrapped in try/except; the persistence loop that follows (iterating `arrivee.chevaux` and calling `repository.ajouter_performance_historique_backfill`) is NOT guarded. A single persistence-layer exception (DB error, unexpected type) on one day/hippodrome propagates uncaught out of the whole function, aborting a potentially multi-month/multi-year backfill run entirely instead of skipping just that day/hippodrome — the opposite of the documented "un jour/hippodrome en échec est avalé, la boucle continue" resilience the fetch-layer guard already provides.
  evidence: Edge-case-hunter review of Story 2.3, findings #6/#7. Not fixed here — `backfill_historique.py` is read-only this story; worth the same follow-up fix as Story 2.2's `get_programme` finding (wrap the persistence loop in its own try/except per iteration), ideally in the same follow-up pass since both are instances of the identical asymmetry pattern across Epic 2's three jobs/repository call sites.

- source_spec: `_bmad-output/implementation-artifacts/spec-2-3-backfill-historique-test-coverage.md`
  summary: PRODUCT-RELEVANT — `backfill_cheval_par_hippodromes_connus` persists `date_course=arrivee.date` (backfill_historique.py:108), the network-derived, parseable-as-`None` date (`open_pmu_client._parse_date_brute` returns `None` when the response's `date` field is missing or unparsable). Unlike `backfill_plage` (which always persists the loop's own guaranteed-non-null `jour`), a `None` `date_course` here defeats the deduplication this job relies on: SQLite's unique index on `(cheval_id, date_course, hippodrome)` treats every `NULL` as distinct from every other `NULL`, so `on_conflict_do_nothing` never fires — repeated calls for the same cheval/hippodrome combo where date parsing failed would insert duplicate `PerformanceHistorique` rows indefinitely instead of being deduplicated, inflating `total_ajoutees` and the stored history.
  evidence: Edge-case-hunter review of Story 2.3, finding #8, cross-referenced against `open_pmu_client._parse_date_brute` (returns `None` on missing/unparsable date) and the unique-index NULL-semantics comment already present in `repository.py`. Not fixed here (read-only story) — worth a dedicated fix (skip persisting a row when `arrivee.date is None`, with a log warning) in a follow-up story.

- source_spec: `_bmad-output/implementation-artifacts/spec-2-3-backfill-historique-test-coverage.md`
  summary: Lower-priority test gaps left open after Story 2.3's review: `main()`'s CLI validation (`--date-fin >= --date-debut`, argparse wiring) has zero test coverage, consistent with `ingest_daily.py`'s `main()` also being untested in Story 2.2; no test exercises an `Arrivee` with an empty `chevaux` list mixed into a non-empty response; no test covers a `cheval` filter matching zero horses across an entire multi-day range (only within a single day); no test covers the same cheval name appearing twice within one `Arrivee`'s `chevaux` (an intra-response duplicate, as opposed to the cross-call duplicate already covered); no test uses `caplog` to confirm `logger.exception` is actually called on the swallowed-exception paths (same class of gap as Story 2.2).
  evidence: Blind-hunter + edge-case-hunter review of Story 2.3. The highest-value gap (pass-through field mapping to the persisted row, confirmed as a genuine verification gap by all 3 lenses independently) plus two cheap-to-close edge cases (`date_fin < date_debut` called directly, empty `hippodromes` list) were fixed directly during this story's review; these remaining ones are lower-value/narrower branches.

- source_spec: `_bmad-output/implementation-artifacts/spec-3-3-transparency-indicators.md`
  summary: The régularité gradient ("Très régulier"/"Régulier"/"Moyen"/"Irrégulier", `EXPERIENCE.md`/`DESIGN.md` `regularity-indicator`) is still not implemented. Its formula is not defined anywhere available (cahier des charges, the two HTML prototypes, or the backend engine) — only the three FR-4 transparency labels ("🐎 Inédit"/"Données non saisies"/"Historique court (N)") have a defined source of truth, now centralized in `mobile/lib/widgets/transparency_label.dart`. `results_screen.dart`'s old private `_regularite()` fabricated a "Régulier" label for any horse with `nbPerfs >= 3` — that behavior was removed (not replaced) by this story per its explicit "Never" boundary; full-history horses now render no badge at all ("silence = signal", `EXPERIENCE.md` State Patterns).
  evidence: spec-3-3-transparency-indicators.md, "Never" boundary: "Do not implement the régularité gradient ... its formula is not defined anywhere ... log it to deferred-work.md instead of guessing." Needs the actual scoring formula (presumably derived from variance/consistency of past `rang` or `note` across `recenceStd` window) defined by a human or a future spec before implementation — worth a dedicated story once that formula exists in the cahier, backend, or is explicitly specified by a stakeholder.

- source_spec: `_bmad-output/implementation-artifacts/spec-3-3-mandatory-recalc-on-removal.md`
  summary: PRODUCT-RELEVANT — only horse-roster mutations (add/edit/remove) mark `resultsProvider` stale; changing the target race's terrain/niveau/distance (`race_config_screen.dart`) or any engine parameter (bankroll, Kelly fraction, récence mode, etc. in `settings_screen.dart`) after a calculation leaves `isStale` at `false`, so the results screen keeps showing a ranking computed against inputs that no longer match the current config, with no visual signal at all — the exact "silently obsolete" failure mode FR-6 exists to prevent, just on a different trigger than the one this story scoped.
  evidence: Blind-hunter review of spec-3-3-mandatory-recalc-on-removal.md. Deliberately out of this story's scope (title and Code Map are explicitly "when the horse roster changes"; `race_config_screen.dart`/`settings_screen.dart` aren't in its Code Map) — worth a follow-up story extending the same `markStale()` mechanism to `raceConfigProvider`/`engineParamsProvider` mutations.

- source_spec: `_bmad-output/implementation-artifacts/spec-3-3-mandatory-recalc-on-removal.md`
  summary: `AnalyseResult` (`mobile/lib/providers/results_provider.dart`) has no `copyWith` — both `calculer()` and `markStale()` reconstruct it via positional constructor args, repeating all fields by hand. Not a bug today (both call sites are correct and tested), but if a field is ever added or reordered, nothing forces `markStale()` to keep carrying it forward correctly — silent, easy-to-miss drift risk.
  evidence: Blind-hunter review of spec-3-3-mandatory-recalc-on-removal.md. Low priority (cosmetic today) — worth adding a `copyWith` the next time `AnalyseResult` gains a field, as a concrete trigger.

- source_spec: `_bmad-output/implementation-artifacts/spec-3-3-mandatory-recalc-on-removal.md`
  summary: The stale-result banner on `results_screen.dart` is purely visual (color + icon + text) with no `Semantics(liveRegion: true, ...)` or equivalent — a screen-reader user isn't proactively notified that the displayed ranking just became stale, only a sighted user scanning the screen would notice.
  evidence: Blind-hunter review of spec-3-3-mandatory-recalc-on-removal.md. Consistent with the epic's accessibility floor (EXPERIENCE.md/DESIGN.md: "no signal carried by color alone") but not explicitly required by FR-6 or this story's frozen intent — worth a dedicated accessibility pass across all of Résultats' state signals (also covers the FR-4 transparency badges), not just this one banner.

- source_spec: `_bmad-output/implementation-artifacts/spec-3-3-mandatory-recalc-on-removal.md`
  summary: While a result is stale, the race-info summary card above the ranking (`'${result.resultats.length} partants...'` in `results_screen.dart`) still reports the pre-edit partant count from the frozen `AnalyseResult`, not the live `horsesProvider` roster size — only the banner signals staleness; the rest of the header looks normal/trustworthy.
  evidence: Blind-hunter review of spec-3-3-mandatory-recalc-on-removal.md. Not required by this story's acceptance criteria (banner + old ranking visible beneath it) — worth revisiting when Story 3.6 (Résultats screen polish) is tackled, to decide whether every stale-derived field should carry its own stale treatment or whether the single banner is sufficient.

- source_spec: `_bmad-output/implementation-artifacts/spec-4-1-vision-client-adapter.md`
  summary: NEEDS HUMAN VERIFICATION before production use — `max_tokens: 2000` (inherited unchanged from the JS prototype) is shared by both `extract_fiche_cheval` and `extract_programme`, but a programme extraction (potentially 15-20+ horses × up to 6 performances each) is structurally far larger than a single fiche, risking response truncation for busy race cards. Separately and unverified: a reviewing subagent claimed Claude Sonnet 5 may run extended/adaptive thinking by default (consuming tokens from the same `max_tokens` budget as the JSON answer) even when the `thinking` param is omitted — this claim was NOT independently confirmed against current Anthropic API documentation and should not be treated as fact, but is worth checking before Story 4.2 wires real traffic through this module, especially since AD-3 already flags the model string itself as something to re-verify rather than assume.
  evidence: Blind-hunter review of Story 4.1, findings #1 and #10. Not fixed here — raising `max_tokens` (and possibly setting `extract_programme`'s value higher than `extract_fiche_cheval`'s) is a one-line change once a human confirms the actual current API behavior; guessing a "safe" number without that confirmation risks either wasted cost or continued truncation risk.

- source_spec: `_bmad-output/implementation-artifacts/spec-4-1-vision-client-adapter.md`
  summary: Lower-priority gaps left open after Story 4.1's review, all in `vision_client.py`: `media_type` is never validated against a known-supported list before `_enforce_budget()` runs, so an unsupported/typo'd media type still consumes one budget unit before the Anthropic API itself rejects it; multiple `type: "text"` content blocks in a response are joined with `"\n"` before JSON-parsing (mirrors `docs/analyse_hippique_ia.jsx:339` exactly — inherited from the reference prototype, not a regression — but a model that splits its JSON output across two text blocks could theoretically corrupt it via an injected newline); the fence-stripping `.replace()` calls remove ALL occurrences rather than just the first pair, and the daily-counter date-reset check uses `!=` rather than `<` (both differ only in contrived/clock-skew scenarios, not realistic ones); there is no INFO-level logging on the success path (deliberate — avoids any risk of an API key or payload leaking into logs at any level — but means no per-call cost/audit trail for an operator tracking real spend on this first paid vendor integration); `VISION_DAILY_CALL_LIMIT` is re-read from `os.environ` on every call rather than cached, so a persistently misconfigured (non-integer) value logs a fresh ERROR on every single call rather than once; empty `file_bytes = b""` is not rejected locally before consuming a budget unit and making a network round-trip.
  evidence: Blind-hunter + edge-case-hunter review of Story 4.1. The higher-value findings from the same review (budget consumed before the API-key check; a TOCTOU race on the module-level counter under threadpool concurrency; no test on outgoing request headers; the daily-limit boundary only ever tested via direct global-state manipulation, never real sequential calls; a top-level JSON array response; a non-dict entry inside a `perfs` array) were fixed directly (2 source-code changes + 8 new tests) during this story's review.
