---
scope: Backend FastAPI + mobile Flutter, invariants for epics/stories breakdown
purpose: build-substrate
altitude: feature
updated: 2026-09-04T02:34
---

- (decision) Paradigme nomme: hexagonal / ports-et-adaptateurs [ADOPTED - ratifie sur code existant, pas invente]. Domaine pur = engine/scoring.py + engine/combinatoire.py (aucune dependance FastAPI/SQLAlchemy). Ports pilotes (driven) = pmu_client.py, open_pmu_client.py (adaptateurs source de donnees, format JSON brut jamais expose hors du module - deja impose par cahier backend 5.1), repository.py (port persistance). Adaptateurs pilotants (driving) = routes_*.py (FastAPI). Confirme explicitement par l'utilisateur.
- (decision) AD: parite moteur backend/mobile appliquee par fixture JSON partagee, pas par discipline humaine. Fichier unique fixtures/engine_cases.json a la racine du depot (territoire neutre, ni backend/ ni mobile/ ne le possede) charge par pytest (backend/tests/test_scoring.py) ET par la suite Dart (mobile/test/engine/scoring_test.dart), au lieu de deux suites ecrites a la main independamment. Portee precisee: le fixture couvre les cas a entree fixe / sortie fixe ou tolerance numerique (ex. sommes de probabilites ~1, bornes du lissage bayesien, exemple value/Kelly, equivalence terrain/niveau=None) - PAS les tests de proprietes comparatives entre plusieurs resultats dans le meme test (ex. disqualification vs chute, croissance du ratio ordre/desordre par profondeur), qui restent ecrits a la main par langage car ils encodent une propriete qualitative dans l'assertion elle-meme, pas une valeur magique sujette a derive silencieuse. Choix de l'utilisateur (option A vs statu quo B), motive par le contexte du projet: stories construites par des agents a des moments differents, pas une equipe qui se coordonne en continu.
- (decision) AD: vision_client.py [ADOPTED, ratifie depuis cahier backend section 5] suit le meme pattern adaptateur que pmu_client.py - seul module connaissant le format de requete/reponse du fournisseur vision (Anthropic Claude, deja choisi par l'utilisateur plus tot dans la session), expose des fonctions normalisees type extract_fiche_cheval(image) / extract_programme(image), jamais appele directement depuis routes_analyse.py ni aucun autre module. Cle API Anthropic en variable d'environnement backend uniquement (deja une NFR du PRD 8, jamais cote mobile).
- (decision) AD: garde-fou de cout mecanique pour l'extraction vision, decide maintenant plutot que differe (choix explicite de l'utilisateur) - un compteur d'appels journalier (memoire ou table BDD simple) avec un plafond par defaut conservateur configurable via variable d'environnement (ex. VISION_DAILY_CALL_LIMIT), qui bloque l'appel et log bruyamment au lieu de continuer silencieusement au-dela du plafond. Le chiffre exact du plafond et le budget associe restent une Open Question du PRD (9.4, 11.4) - seul le mecanisme est fixe ici, pas la valeur.
- (decision) Reviewer gate (rubric walker + tech-currency + adversarial) execute en parallele. Triage: AD-2 elargi pour couvrir les tables de constantes (terrain/niveau/incidents/recence) en plus des cas de test et des defauts, avec un carve-out explicite pour les paris Monte-Carlo (non reproductibles bit-a-bit entre numpy et dart:math meme a seed egal - tolerance statistique seulement, jamais egalite exacte). Nouvelle AD-4 sur le contrat de reponse API type (routes_analyse.py expose actuellement HorseAnalysis.__dict__ brut au lieu du schema HorseOut - incoherence deja presente dans le code, pas hypothetique - logguee aussi dans deferred-work.md comme bug concret a corriger). Nouvelle AD-5 sur la representation fil-de-fer (wire) de terrain/niveau: labels francais canoniques, jamais des floats pre-resolus, avec distinction explicite entre 'neutre car absent' et 'label non reconnu'. Convention de nommage assouplie: elle s'applique au coeur moteur (HorseAnalysis/Performance et son miroir JSON), pas aux schemas API/DB qui servent un contexte different (ParticipationOut vs HorseIn) - unification pas requise la ou le mapping explicite existe deja.
- (decision) Tech-currency: SQLAlchemy tagué [ASSUMPTION] au meme titre que FastAPI (meme risque, meme absence de pin). Flutter/Dart verifie (Flutter 3.47.1/Dart 3.13.1, compatible avec pubspec sdk ^3.12.0) - hedge remplace par la version verifiee. Python 3.12 note en maintenance securite uniquement (EOL oct 2028, pas un bloquant). Riverpod ^3.4.2, sqflite ^2.4.3, dio ^5.11.0 verifies actuels, versions exactes ajoutees. freezed/json_serializable ajoutes au Stack (deja dans pubspec.yaml, prescrits par le cahier mobile pour la parite JSON). Fournisseur vision (Anthropic Claude) explicitement marque comme decision de cette session (choix utilisateur), pas une source PRD/cahier - ligne Stack ajoutee avec note de re-verification future du nom de modele exact.
- (event) spine finalized
