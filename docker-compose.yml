services:
  api:
    build: ./backend
    ports:
      - "8000:8000"
    environment:
      - DATABASE_URL=sqlite:////app/data/hippique.db
    env_file:
      - path: .env
        required: false
    volumes:
      - hippique_data:/app/data
    restart: unless-stopped

  ingestion:
    build: ./backend
    command: ["python", "-m", "app.jobs.scheduler"]
    environment:
      - DATABASE_URL=sqlite:////app/data/hippique.db
      - INGEST_HOUR=6
    env_file:
      - path: .env
        required: false
    volumes:
      - hippique_data:/app/data
    restart: unless-stopped

volumes:
  hippique_data:
